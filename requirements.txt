# live_trader.py — XGBoost Trading Bot
# Install: pip install -r requirements.txt

# ── Core ML & data ────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0

# ── Excel trade log ───────────────────────────────────────────────────────────
openpyxl>=3.1.2

# ── FIX mode (headless / VPS) ─────────────────────────────────────────────────
yfinance>=0.2.40

# ── cTrader Open API (live balance + trade history) ───────────────────────────
ctrader-open-api>=0.9.0

# ── Firestore trade logging (optional — install manually on VPS/desktop) ─────
# firebase-admin>=6.5.0

flask==3.0.3