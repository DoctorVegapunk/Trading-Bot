# live_trader.py — XGBoost Trading Bot
# Install: pip install -r requirements.txt

# ── Core ML & data ────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0

# ── Excel trade log ───────────────────────────────────────────────────────────
openpyxl>=3.1.2

# ── FIX mode (headless / VPS) ─────────────────────────────────────────────────
yfinance==1.4.1

# ── cTrader Open API (live balance + trade history) ───────────────────────────
# Installed via --no-deps in Dockerfile to avoid protobuf 3.20.1 conflict with google-api-core
# ctrader-open-api==0.9.2
# --no-deps deps required at runtime:
Twisted==24.3.0
pyOpenSSL==24.1.0
inputimeout==1.0.4

# ── Firestore trade logging (always-on) ──────────────────────────────────────
firebase-admin==7.2.0

flask==3.0.3